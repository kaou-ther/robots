<%= "Your order is cooking" %>

<div id="orders">
  <% @orders.each do |order| %>
    <p>
      <%= link_to order_path(order), order %>
    </p>
  <% end %>
</div>

<button type="button" class="btn btn-outline-primary"><%= link_to "Place a new order", new_order_path  %></button>

<%= link_to "Home", root_path %>
