<div class="border-bottom border-black bg-white">
  <div class="container d-flex py-1">
  <p class= "btn small-btn"><%= link_to "Home", root_path, class: "link-color" %></p>
  <button type="button" class="btn btn-outline-primary"><%= link_to "Place a new order", new_order_path, class: "link-color"  %></button>
  </div>
</div>

<%= "Your order is cooking" %>

<div id="orders">
  <ol>
  <% @orders.each do |order| %>
    <p>
      <li class= "btn"><%= link_to order_path(order), order, class: "link-color" %></li>
    </p>
  <% end %>
  </ol>
</div>
